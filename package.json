{
  "name": "confidentialkit-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "Open-source developer toolkit that makes Solana Token-2022 Confidential Balances usable.",
  "license": "MIT",
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r --if-present lint",
    "typecheck": "pnpm -r typecheck",
    "fork:up": "bash scripts/surfpool-fork.sh",
    "clean": "pnpm -r clean && rm -rf node_modules"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsup": "^8.3.5",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
